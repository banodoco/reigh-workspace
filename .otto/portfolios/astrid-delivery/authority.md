# Authority and handover contract

Recorded from the user's September 10 instructions and subsequent steering.

- **Latest source/GPU instruction:** publish validated latest intended local Astrid and Banodoco Runtime source to their respective main branches. The receiving orchestrator adopts the running GPU/UE project immediately by recovering its existing conversation; at its verified checkpoint (previously P7 CPU closure; user later said R7), delegated workers first preserve/compose its existing work, then merge current main into isolated continuation branches and test the combined revision set. See gpu-project-takeover.md. This overrides the earlier Runtime handover-branch-only publication decision, not source safety or project acceptance gates.

- **Preparation now:** build this first meta-handover, preserve/publish relevant current source and plans to their existing GitHub repositories, and have one independent Astra high agent improve it from the receiving coordinator's perspective. Do not launch the portfolio while preparing the package.
- **Receiving mode:** delivery. The selected finish instruction is all agreed work implemented, validated/tested, integrated and merged to `main` in affected repositories, with remote identity verified. The recipient may launch project-manager Codex windows, delegate workers, continue authorized work, coordinate merges and push normally within this scope. This explicit user mandate supersedes older planning-only prose for the recipient; record it in each existing active run before execution without resetting counters.
- **Current program:** Astrid unified-execution; handoff label `megado-unified-v3`; current workstream P7 CPU closure (user-confirmed). Reigh is an integration surface. Current exact source, evidence and outstanding obligations come from its live handoff, not older preserved phase counts.
- **Handover checkpoint:** freeze P7 CPU closure, capture main plus relevant dirty/untracked code across repositories, reconcile into one verified continuation composition, then release overlapping new work. If a current-main change is required for P7 itself, its manager accounts for that input before freezing.
- **Source custody override:** the user chose an explicit post-P7 integration/continuation branch and final merge to main. This replaces an incompatible earlier main-only/no-new-branch rule *for this agreed integration workflow*. Record the amendment in the DB/GEN/UE active goals before affected changes. Preserve all input branches/dirty work. This does not grant permission to reset, force-push, delete history, or mix unrelated work.
- **Document class/location:** the user explicitly requested manager-level documents inside `.otto`. The selected portfolio control root is `.otto/portfolios/astrid-delivery/`, separate from individual `.otto/runs/`. This overrides the handover skill's default tracked `docs/projects/` location. Only the explicitly selected portable portfolio package is published; never force-add all `.otto` control state.
- **Visibility/destination:** the handover host is the existing public `banodoco/reigh-workspace` repository, on `handover/astrid-portfolio-20260910` based on its recorded remote main. The user's request to preserve/push current main and relevant dirty code applies separately to source repositories, especially Astrid. Source publication refs and exclusions are recorded in source-publication.md. Public planning material excludes credentials, personal-machine paths and raw operational logs. Existing unrelated private repositories are not made public.
- **Receiving-machine override:** the user confirms execution is on a different MacBook with 8 GB RAM and the running GPU conversation is there. Recover that destination's existing owner/control roots; the preparation-machine search cannot establish its state. Fetch pinned source/skills using recipient credentials and run the documented local-only skills sync. No credentials, local dirt, installed environments or conversations transfer automatically. A required missing source/credential is an explicit prerequisite, not permission to guess or restart the project.
- **Thread continuity:** one ongoing orchestrator thread and one ongoing manager thread/window per project. Send concise meaningful updates there. Avoid fresh replacement conversations on every check, parallel managers for one project, and recursive review panels.
- **Checkpoint investigation:** at every checkpoint deploy bounded Luna evidence investigators across active work. The orchestrator judges their findings and decides proceed, hold or redirect within its mandate; required project reviews and oracle authority remain intact. See operations.md for the lightweight evidence contract.
- **Hourly supervision:** use the user-selected **wakeup-loop** name and same-thread awaited waiting mechanism, with checks about once per hour and earlier reactions to handoffs/failures. No custom hourly background scheduler or watcher. A wait must be awaited in the current turn; it does not restart a dead thread.
- **Added project:** the user explicitly added ephemeral-derived-artifact-lifecycle (E2) to the portfolio. It gets its own manager and preserves its shared remaining oracle budget with V1: two calls total across both, not two per manager. Its final integration follows V1 acceptance. Existing protected/durable media must survive lifecycle operations.
- **Limits retained:** each project retains its required tests, review stages, consumed counters, provider/GPU restrictions and data boundaries. No extra paid GPU resources, deployments, production cutovers, migration of old realms or destructive cleanup is authorized by this package.
- **Approved follow-up:** add explicit V1 fixture readiness, three ready implementation lanes with a conditional fourth, and one separately managed Integration & E2E workstream. This manager coordinates the exact multi-repository composition, final practical test and main promotion under the orchestrator; it adds no product implementation scope, review panel, oracle allowance or provider spending. Banodoco Workspace Runtime is a first-class separate repository, not subsumed by Astrid. Follow repositories.md.
- **Pure orchestration override:** the user selects GPT-6 Astra low for the top-level coordinator, GPT-5.6 Luna xhigh for each persistent project manager, and a separate sparingly invoked GPT-6 Astra high meta oracle. Top-level and project managers delegate substantive investigation, implementation, testing, bookkeeping and Git/setup work; they own briefing, judgment, communication and supervision. Begin with a visible instruction read-back, not a new approval gate. Preserve existing worker/reviewer bindings, project-local oracle policy and all counters; the meta oracle still has three total calls. Follow orchestrator-contract.md and reconcile live manager bindings without duplicate owners.

User instructions outrank snapshots and skill defaults. Ordinary reversible implementation/scheduling choices within this mandate need no new permission. Escalate only a concrete unresolved choice outside it; keep independent work moving. Do not interpret a model review as permission to waive failed behavior or enlarge budgets.

The portable project files are frozen handover inputs. On the receiving machine keep each recovered existing control root authoritative and map the recorded relative paths to verified destination roots. If a new-project root is missing, initialize it once from its snapshot, marking inherited state unverified; never overwrite a newer run. Recover the GPU owner before reconstructing its control state. The top-level meta run has one active `run.yaml`; copied package YAML is its seed, not a second live configuration.
