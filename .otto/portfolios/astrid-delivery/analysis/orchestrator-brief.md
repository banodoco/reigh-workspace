> Frozen reference snapshot. Current state and receiving authority are governed by the package START-HERE and authority.md; preserve the existing live run. Paths below are source references, not missing package requirements.

# Orchestrator brief — five existing programs, one compatible composition

Prepared 2026-09-10 by an independent Astra HIGH review with bounded Luna investigations. This is the practical entry point; [sequencing-review.md](sequencing-review.md) explains the challenges and [portfolio-plan.md](portfolio-plan.md) retains the detailed boundary map. Original run authorities govern execution. This brief creates no run, execution permission, task completion, review credit or provider budget.

## Start here

Keep DB, GEN, V1 and RRP as separate projects alongside the **Astrid unified-execution project** (UE). The user identifies its execution-plan label as **`megado-unified-v3`** and current workstream as **P7 CPU closure**. It spans Astrid, Runtime, Reigh app/worker integration and VibeComfy; Reigh is an integration surface, not the project name. Appoint or recover one project manager per authority below; managers delegate most investigation, implementation and validation to their configured leaf workers. The orchestrator coordinates their handoffs, source custody and integration.

The first dispatch is a bounded readiness return from each manager. The four new run artifacts say `planning_only`. For UE, recover the **current megado-unified-v3/P7 handoff and accepted source/contract receipts**; do not restart P0–P3 from the old Megaplan wrapper's launch error. The user's current-workstream correction is authoritative; exact accepted source is still to be evidenced. Under today's authorization only read-only research and report writing are allowed. A later execution mandate enables eligible work without asking again for routine assignments, file turns or focused corrections.

After that mandate and relevant custody checks, continue UE's **remaining P7 CPU closure** on its accepted candidate while DB T1→T2→T3, V1-01→V1-02 and RRP-01→RRP-04 progress on available source. Agree GEN T1 requirements during DB format design. Retrieve accepted S/H and existing manifest/publication contracts from the current UE handoff; reuse them rather than rebuilding the manager. Release each ready task immediately. GEN T3 consumes the verified current session contract when relevant. New DB/GEN changes entering UE's chosen composition require a recorded integration decision and affected evidence, not an automatic restart of completed phases.

Done means each project's original criteria and declared reviews are satisfied on identified source, required cross-project evidence agrees on the delivered composition, and remaining live/rollout limitations are explicit. GPU additionally requires v3's 45/45 or an honest terminal, tracked source, cleanup and final evidence. An honest GPU terminal is a supported stopping outcome, not successful GPU delivery. Independently accepted DB/GEN/V1/RRP work can remain accepted at its stated source/scope even if GPU warmth cannot be proved.

## Authorities and managers

All paths below are relative to `<workspace>`. `DB-T1` and `GEN-T1` mean original `T1` in their respective run; `UE-P2` means original v3 `P2`. These are disambiguation labels, not new IDs. Earlier reports use “GPU” for UE; that is historical shorthand, not the project's current name.

| Manager | Exact authority root and first reads | Original order and finish |
|---|---|---|
| DB PM | `Astrid/.otto/runs/runtime-database-clean-break-20260910/`: `agent_goal.md`, `northstar.md`, `tasklist.md`, `run.yaml`, `status.md`, `source-state.md`, `implementation-criteria.md`; architecture `Astrid/docs/architecture/runtime-database-clean-break-plan.md` | T1→T2→T3→foundation PASS→T4/T5→T6→T7→final review; full C1–C8. |
| GEN PM | `.otto/runs/astrid-generation-path-alignment-20260910/`: `agent_goal.md`, `northstar.md`, `tasklist.md`, `run.yaml`, `status.md`, `source-state.json`, `plan.md`, `oracle-D1.md` | T1→T2→publication_contract PASS→T3→T4/T5→T6→final review; full C1–C9. Warm-session migration remains excluded from GEN. |
| V1 PM | `Astrid/.otto/runs/timeline-visualization-fixes-20260910/`: core files, `source-provenance.md`, `source/selected-dirty.patch`, `references/timeline-visualization-fixes-plan.md`, `test-project.md` | V1-01→02→03→04; V1-05 and 06 after 02; all blocking deterministic gates and real fixture before UX-01→02→03→04. |
| RRP PM | `Astrid/.otto/runs/runtime-render-path-reliability-20260910/`: core files, `source-state.md`, `plan.md`, `implementation-criteria.md` | RRP-01→02/03/04/05; RRP-06 after 04/05; RRP-07 after 02–06. No currently configured final review stage; resolve the delivery-stage instruction below once. |
| UE PM | **Current:** user-confirmed `megado-unified-v3`, P7 CPU closure; exact current handoff/control-root and accepted composition still to locate. **Historical authority:** `.megaplan/plans/complete-the-astrid-gpu-20260909-1540/anchors/north_star/plan.md`, `idea_snapshot.md`; historical wrapper only: `state.json`, `phase_result.json`. Predecessor receipts: `reigh-app/.otto/runs/astrid-gpu-single-path-20260905/`. | Continue the current P7 CPU closure. Preserve ratified P0→P7 order and original B03–B05/CR2/B06 acceptance obligations; verify current receipts/counters before declaring gaps. Never reset to the historical 22/1/22 checkpoint or rerun P0–P6 simply because the old wrapper failed. |

Recover an existing manager's durable context before appointing a replacement; do not create two active managers for one root. A run ID, lock file or stale `active` label does not identify a currently operating agent. Validate the intended continuation mechanism and execution mandate before contacting/resuming an existing thread. Wrapper repair is a separate control-plane issue; missing wrapper launch seed neither proves product failure nor justifies resetting planning, tasks, counters or source lineage.

The four Megado roots configure Luna medium coordinators, Luna high normal workers, Sol high hard workers/oracles except V1's Astra medium oracle, and their stated reviewers. GPU retains v3's Astra low direction, Sol P0.b/P1/P2 implementation and independent Sol reviews, Luna adapter/harness/evidence work. This Astra HIGH planning review does not change those routes. Managers read the applicable Megado execution mechanics before delivery custody setup; workers/reviewers remain leaves.

## First manager dispatches

Use the common template below, adding the appropriate row. A readiness return is preparatory evidence; it does not close an original task that also requires implementation. Work can proceed within today's research scope while dependent execution stays dormant.

| Recipient | First bounded assignment / required return | First implementation eligible later |
|---|---|---|
| DB PM | T1 readiness: identify actual Runtime **main** source, protected dirty deltas and retained fixtures; map exact fresh format/admission/transaction needs with GEN; identify activation interface with RRP. Report unresolved custody precisely. | T2→T3, including RRP-02's diagnostic requirements through one Runtime implementation. |
| UE PM | **P7 handoff readiness:** identify current `megado-unified-v3` control root/manager, remaining CPU closure rows, accepted S/H and HC-04/protocol/manifest receipts, exact multi-repo candidate, source custody, CR2/final-freeze state, remaining reviews and live allowance. Mark any historical P0–P6 obligation unresolved only on concrete current evidence. | Continue remaining authorized P7 CPU closure in original order. Hand off accepted session/publication primitives to the other PMs immediately when evidenced. Do not repair/resume the stale wrapper or reconstruct an already completed backend as a first step. |
| GEN PM | T1 readiness: group/ordinal/partial policy and envelope examples; decide whether durable SQL changes are necessary; request shared settlement/filename/client handoff from DB PM; map independent adapters and current host dirt. | T1 contract edits under agreed custody; T2 after F; T3 after publication_contract PASS on the chosen candidate. |
| V1 PM | Reconcile the selected dirty patch; map parser/identity/clock gaps; specify fixture/capture capability and exact disjoint files. Share selection invariants with RRP without importing its weaker latest policy. | V1-01→02; fixture creation may be delegated early after delivery authority, while actor UX waits for real evidence gates. |
| RRP PM | RRP-01 readiness: trace diagnostics, activation, scoped resolver and filename loss; send DB and GEN concrete requirements; inventory availability of the exact historical Minkhole bytes and managed access without operating the realm. | RRP-04 after 01; RRP-02 contribution through DB; RRP-05 field/local-render work under serial custody; RRP-03 consumes accepted activation behavior. |

```text
You are the project manager for <exact existing authority root>.
Mode/authorization: <research only OR cited delivery mandate>; preserve original roles/counters.
Outcome: <original task IDs + concrete user behavior>; required criteria: <original IDs>.
Baseline: <repository, path, HEAD, protected dirty manifest>; allowed writes: <exact closure>.
Inputs: <accepted handoff versions/receipts>; missing inputs: <specific dependent work only>.
Delegate investigation/implementation/validation to configured leaf workers; own process/evidence.
One worker has one directing PM. Shared-boundary requests go to its owning PM, not its workers.
Return: findings/completed scope, exact source+contract identities, evidence/receipt links,
review and resource balances, next ready original task, blockers with owner and proposed remedy.
Do not expand acceptance, alter custody mandates or consume an undeclared review/provider budget.
```

## One writer and one directing manager

The Runtime boundary owner is the **DB PM**, not a new integration project. GEN retains generation semantics and acceptance; RRP retains diagnostic/launcher acceptance. A shared Runtime patch is assigned to one worker under DB PM with the contributing task IDs and assertions attached. DB PM delivers one immutable patch/evidence handoff; GEN/RRP PMs credit their own criteria only after checking that evidence. They do not issue competing worker instructions or mark each other's tasks complete.

Astrid integration custody is a **seat held by an existing PM**, not an independent implementation authority. Preserve UE PM's current P7 closure custody; identify accepted host/session source and transfer only the agreed common-publication turn to GEN PM for GEN-T3/RRP-05/V1-06 requirements. Any remaining session repair stays with UE PM. DB PM directs its DB-T6 caller closure; V1 and RRP direct their local changes. The custodian records source composition and exclusive write turns; the PM named on the patch directs implementation. A separate integrator, if used, is a leaf executing a specified source transfer under that PM, without scope or acceptance authority.

Before a shared edit, the responsible PM supplies intended behavior, contributing original IDs, exact files/symbols, input source and consumer assertions. The orchestrator settles competing file turns; the custodian confirms the preimage. Afterward, record the patch, resulting source and affected evidence, then release custody. Unexplained source drift pauses the overlapping write and triggers read-only reconciliation. Serial writing alone is insufficient: overlapping tests must use a stable candidate, or all intervening changes must be recorded and affected results rerun. Generated clients have one contract lineage and are regenerated, never hand-merged.

Custody is currently unresolved for shared Runtime/Astrid source. DB's explicit main/no-branch/no-worktree amendment stands; GEN asks for isolated custody; GPU P1 explicitly requires an isolated clean Vibe worktree. Preserve their scopes. A proposed task/diff-isolation interpretation for shared GEN/DB edits must be recorded and accepted as compatible with existing authority before use. An oracle can resolve architecture/source interpretation within its remit; it cannot override an explicit user mandate. Do not switch the dirty Runtime feature checkout to main, discard dirt, infer that recovered GPU commits are integrated, or transplant uncommitted content implicitly.

## Minimal cross-project dependency ledger

These are handoff aliases over existing tasks, not another tasklist. UE is currently at P7 CPU closure per the user; P0–P6 rows below describe provenance/dependency contracts to **retrieve and verify**, not a restart queue. Their historical pending marks are not current status. Source-specific acceptance still requires current receipts. Original intra-project dependencies remain mandatory.

| Producer / handoff | Consumer | Required release evidence and owner |
|---|---|---|
| DB-T1 + GEN-T1 + RRP-01 + UE-P0 contract/source design | Each project's first shared edit | Exact source custody; proposed SQL shape vs wire envelope vs session capability; output identity requirements. Joint input exchange, **not UE-P0 completion before all T1 work**. Orchestrator records ownership. |
| **F:** DB-T2/T3 + configured foundation PASS | GEN-T2; DB-T4/T5; dependent RRP-02/05 integration | DB PM: exact format/verifier/admission/transaction revision and retained refusal, owner-race, rollback/receipt/CAS evidence. GEN-T1 supplies anticipated schema needs before F freezes. |
| **P:** GEN-T1/T2 + publication_contract PASS; RRP-05 Runtime filename requirements | GEN-T3 and output consumers | DB PM produces serialized Runtime protocol/client artifacts; GEN PM accepts publication semantics: task/group/ordinal identity, partial rules, output verification, atomicity/replay/stale/cancel behavior; filename distinct from port/object/digest. Usable generated clients and fixtures are part of P. |
| DB-T4/T5 activation contract | RRP-03; DB-T6 | DB PM owns verification/replacement/catalog/credentials/epoch/readiness. RRP PM proves the normal launcher invokes it, restarts and discovers it. Contract design and fixtures can overlap; actual activation integration consumes accepted operation evidence. |
| **L:** DB-T6 consumer closure | Affected GEN/V1/RRP final composed evidence | Current combined server/client/typed-error/caller revision after T4/T5; positive domain proof as well as parity. DB-T6's consolidated regeneration does not prohibit P's earlier necessary generated client. |
| UE-P1 content merge + independent review | UE-P2 | UE PM: reviewed `cb130f1b`/`5b39fea5` content composition with launch ownership and modern configuration consumed by both Vibe modes. An ancestry-only merge does not release P2. |
| **S:** UE-P2 independent review | GEN-T3/shared-host integration and bootstrap **that consumes retained-session changes** | UE PM: reviewed capability/custody operations, session incarnation and admission fence, task spool boundary and CPU fault evidence on exact consumed code. Consumer-specific CPU integration proof is still required. Full P3 inventory is not an extra GEN prerequisite. |
| **H:** UE-P3/M1 with P0.c final | UE-P4 and GPU composition/harness acceptance | Exact session-aware host/harness composition, independent observation, frozen executable case inventory and capacity gates. P0.c is provisional before P1–P3 outputs, final at M1. Complete P3 remains mandatory before P4; other PMs may use S earlier. |
| GEN-T3 accepted publication on chosen source | GEN-T4/T5; affected RRP-05 and V1-06 integration | GEN PM: verified named managed outputs, sync/caller-exited async readback and non-generation controls. RRP/V1 keep their own assertions. One-shot/cloud/fixture evidence can precede S when it consumes no retained-session changes. |
| UE-P4→P5→P6 | Live checkout / retained Wan or embedded claims | UE PM: preserve phase order and physical proof; affected GEN-T5 integration on changed retained adapters consumes their spool/cancel identities. GEN's independent one-shot normalization scope does not become a warm-residency project. |
| RRP-04/05 | RRP-06; then RRP-02–06 → RRP-07 | RRP PM: scoped selection, distinct safe filename/media metadata, verified atomic download/reuse/opener proof; exact real-artifact gate remains explicit. |
| V1-01→02→03→04; V1-05/06 after 02 | UX-01→02→03→04 | V1 PM: real decodable dual-view fixture and capture; all blocking deterministic gates; frozen viewed images/traces and fresh actors. No GPU/adjacent lifecycle completion prerequisite. |
| Accepted F/P/L/S/H and changed producers **actually included in the GPU candidate**, plus v3 P0–P6 and B03–B05/CR2 | UE-P7 final freeze and B06 evidence | Relevant PMs supply consumed revision and affected proofs; UE PM owns final composition decision/evidence. Full GEN-T6 or DB-T7 administrative closure is not automatically a prerequisite to P7. B04-T05/common-manifest requirements are supplied earlier; its P7 acceptance is not a prerequisite to GEN-T3. |

Runtime lease/epoch and GPU admission-generation fencing must both fail closed at settlement. The manager's `generation` is not a GEN generation-row ID. GPU/DB/GEN jointly specify the ordering for cancellation/restart after output creation but before settlement, then assign each implementation change to its existing owner. Warm resident identity excludes prompt, seed, resolution, task and output path; each task still has its own spool and admitted output identities. Do not solve the boundary by adding another task ledger or rewriting settlement digests. See [GPU assessment](gpu-sequencing-assessment.md) and [v3](../handovers/unified-execution/UNIFIED-PLAN-v3.md).

## Ready-set and replanning rules

At startup and each handoff, managers identify the next original tasks or bounded contributions. A candidate is ready when its original predecessors and applicable contract gates are satisfied for the source it will consume, custody/write set is available, required tools/resources exist, and the action fits the cited authorization. Distinguish research, implementation, independent acceptance, combined acceptance and live rollout. Select a maximal useful set of disjoint ready work within available worker capacity; prioritize work releasing shared dependencies. Do not launch five workers merely because five projects exist.

Default from the **current P7 workstream**: finish UE's ready CPU closure while retrieving its accepted S/H, then combine those current contracts with DB F→GEN P for affected GEN-T3 integration. The historical P2/S versus P3/H distinction still matters: S plus consumer CPU proof is the retained-GEN gate; full H was the GPU-P4 gate, not a reason to make GEN wait again. If a current receipt is missing, hold only its consuming claim/transfer and reconstruct provenance; do not infer the implementation is absent. With limited capacity serialize shared writes. Preserve the ratified phase order; no completed phase is reopened without a concrete invalidation. Do not hold P7 for unrelated V1 UX. A change from the **current accepted** UE source needs a recorded composition decision and affected evidence; old pins `70872d03`/`cf9d772be` are historical inputs until the current handoff confirms them. New DB F/P/L are required only when their changed contracts are consumed, never merely because a newer portfolio plan exists.

At a source, format, protocol, capability or output-semantics change, identify consumers by the handoff they actually used. Mark affected handoffs/evidence invalidated for the new candidate; preserve historical evidence and unaffected acceptance. Rebuild generated clients from one revision; rerun meaningful boundary checks and required final checks on the candidate being claimed. A format change updates initializer, exact verifier, identity and backup expectations together. Recreate disposable fixtures explicitly; never weaken verification or migrate existing realms to keep tests green.

For suspected cycles, name the exact edges. Jointly settle the minimal interface, assign one implementation owner, and separate later acceptance from earlier contract availability. Existing resolutions are P0.c-final at M1, P before consolidated L generation, early B04-T05 requirements without early P7 PASS, and DB recovery operation before RRP launcher acceptance. Preserve all original criteria; do not break a cycle by marking an unproved task complete.

## Evidence, review and operating memory

Every handoff needs a compact receipt: producer PM/task IDs; observation time; repository/path/HEAD plus dirty preimage and result hashes or immutable patch; exact SQL format, protocol digest, generated-client/generator hashes, output-envelope and session contract revision as applicable; tested commands, source and fixture IDs; results/artifact links including Runtime task/attempt/receipt/object identities where applicable; reviewer identity/verdict if a configured gate applies; consuming tasks and limitations. Record `not applicable` instead of conflating an absent protocol version with a task/result ID. Shared tests count for two projects only if the source, fixture and assertions satisfy both; each PM retains its own acceptance row.

| Existing review/accounting authority | Preserve exactly |
|---|---|
| DB `run.yaml` | foundation after T3, ≤2 rounds; final after T7, ≤3; oracle ≤3 total minus consumed. |
| GEN `run.yaml` | publication_contract after T2, ≤2 rounds; final after T6, ≤3; oracle ≤3 total minus consumed. No per-task completion review series. |
| RRP `run.yaml`, `status.md:12–14` | oracle ≤3 total minus consumed; currently `review_stages: []`. Status requests choosing stages based on coupling at delivery. Record that choice once under existing authority; recommend shared DB/GEN boundary reviews plus RRP's own required evidence, without an automatic extra series. A final-reviewer model binding alone does not activate a review. |
| V1 `run.yaml`, `agent_goal.md:7`, `test-project.md:16–26` | Two centrally shared remaining oracle calls; separately two capped Astra UX invocations, explicitly **not oracle calls**. Baseline→round 1→precise fixes→fresh replay→round 2→precise fixes→fresh held-out check. No third hidden critique. |
| GPU v3 | Independent Sol P1/P2 reviews and implementer-independent Sol final review; preserve historical routing/counters. This planning review consumes none of those budgets. |

Managers choose local implementation details, split work within original dependencies, assign workers, correct clear in-scope defects and run proportionate required verification. They do not request an oracle for each routine patch. Consequential/contested architecture or acceptance decisions go to the designated existing oracle with evidence and alternatives; resource/custody/authority conflicts go to the orchestrator, and only a needed change beyond existing authorization goes to the user. Exhausted review allowance is reported under the original correction/escalation policy, never reset by renaming a task or adding an integration review.

Managers report immediately on accepted handoff, failed gate, source drift, resource/authority blocker or terminal outcome. While actively executing, use a modest status cadence (for example every 20–30 minutes, adapted to task duration) with five facts: original task, source, evidence delta, blocker/owner, next release. This is a recommendation, not a new background scheduler. Keep original run status authoritative. The orchestrator retains only current composition IDs, the small handoff ledger, active file turns, remaining budgets, unresolved decisions and links to manager reports. Save completed conclusions at seams and compact only through supported host mechanisms when useful; do not carry raw logs in coordinator memory.

## Acceptance, rollout and stopping

Accept projects independently against their full stated scope and source. Combined delivery requires compatible Runtime/server/clients, the composed host/output path, affected V1/RRP readback/fixtures, and required review evidence. GPU-capable combined delivery additionally requires the unified-execution project's applicable physical/final gates. Source changes after GPU acceptance do not inherit the old fire's certification. Choose the final GPU composition deliberately to minimize refires within the original allowance; it need not wait for unrelated UX or documentation.

Preserve these visible limits: V1 needs an actual five-minute video, two byte-distinct renders, hidden truth, decoded tail/cut/spoken traps and confirmed image capture; absent capture is undetermined. RRP-07 names the original Minkhole run/digest and 11,288,161-byte MP4. A synthetic object with copied metadata cannot prove those bytes or managed access. DB makes old realms/backups unsupported and forbids migrating them; the RRP real-artifact evidence route must be resolved without weakening that contract. GEN live coverage needs its separately authorized disposable target and provider cap; fixture results cannot become all-provider live PASS.

GPU retains one pod at a time, ≤2 live attempts per engine/profile cluster, USD 6 aggregate including history, ≤3 hours/pod, preprovisioned models/no downloads, ≥4 GiB local free space and ≤2 GiB generated evidence. Reconcile/sign remaining live allowance and reserve the final run before any fire. A GEN GPU smoke charged to this program cannot evade that allowance. P7 requires provider-observed zero pods; local process exit is insufficient. The missing original `c12-safety-snapshot-20260908/` and unverified offline recovery/budget artifacts remain readiness questions, not proof of permanent failure or permission to fabricate replacements.

On failure, route schema/transaction/recovery defects to DB PM, publication semantics to GEN PM, session/custody/residency to UE PM, scoped open/launcher-consumer defects to RRP PM, and visualization/UX defects to V1 PM. Keep disjoint work moving. GPU verification bypass, unpinned execution behavior, stale/incarnation uncertainty or unproved resource release stops the affected admissions/advancement and invokes v3 fencing/cleanup; unestablishable live allowance stops live work. Ordinary implementation difficulty remains work. When no authorized ready task remains, preserve evidence and report the precise missing authority/resource/contract and its owner; do not spin, relabel failure as success, or launch a substitute program.

Rollout is separate authority. A later authorized cutover accepts the combined exact format and server/client versions, preserves old roots/backups, explicitly provisions a fresh target, proves normal launcher/bootstrap, then proves the dependent host and consumer flows. This research authorizes none of those operations, source commits/pushes, run-mode changes or messages to existing project threads. GPU's existing tracked-source/push completion obligations remain its own program obligations.
