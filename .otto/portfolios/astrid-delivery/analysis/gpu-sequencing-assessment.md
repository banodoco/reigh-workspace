> Frozen reference snapshot. Current state and receiving authority are governed by the package START-HERE and authority.md; preserve the existing live run. Paths below are source references, not missing package requirements.

# GPU successor: sequencing assessment

The recommendation changes at the host boundary: advance the GPU program's accepted P0–P3 session foundation alongside DB foundation and disjoint visualization/render work; make its reviewed session contract an input to GEN-T3 and shared-host integration. Do not finish all P0–P7 before starting the four deliverables. GPU P4–P7 remain mandatory for the GPU program's own physical, warm-residency and final acceptance claims. They are not prerequisites for a deterministic filmstrip, scoped render opener or database verifier.

The successor is an existing upstream/overlapping program, not a fifth tasklist to merge into the four projects. Its ratified phase order and acceptance scope remain intact. The [updated portfolio plan](portfolio-plan.md) contains the operational handoff table; this assessment explains the evidence and changed judgment. Research and report edits only were performed.

## Authority and observed state

The available execution authority is the [captured UNIFIED-PLAN v3](../handovers/unified-execution/UNIFIED-PLAN-v3.md), dated September 9. Lines 3–5 explicitly supersede v2, v1 and the standalone 45-task ladder. Its SHA-256 matches the value captured in `state.json`: `0772f5ac5196b7c0e28d19c1d636f18c26a3cc8c550f9f1a09bd9a4e2ddb7dc0`. The complete anchor, [idea snapshot](../handovers/unified-execution/idea-snapshot.md), state and phase result were read.

The original `c12-safety-snapshot-20260908/` directory is absent in this workspace, including `MEGAPLAN-IDEA.md`, the original v3, `ASTRA-ADJUDICATION-FINAL.md`, fire-19 evidence and recovery archives at their cited paths. `state.json:95–103,154–163` records the original brief as existing when captured; that is historical evidence, not its present availability. The discovery report's original-brief citations cannot be reopened here. The captured anchor supplies the adjudication's incorporated ruling, but this assessment did not independently reread the absent adjudication or verify the four recovered tips' offline bundles.

Use **22 historically accepted closures, one reopened HC-04 correction, 22 pending**, per anchor lines 13–18. Neither the predecessor's 23/45 prose nor discovery of an old receipt closes a pending row. The available HC-04 correction review (original source reference: `../../../reigh-app/.otto/runs/astrid-gpu-single-path-20260905/receipts/R1-B03-T01-hc04-runtime-authority-correction-1-review.json`) confirms REWORK. Its older objection to sha256 object IDs must not reopen the canonical sha256 decision that v3 explicitly preserves (anchor line 14); remaining contract validation/correction still needs acceptance.

The Megaplan **wrapper** is initialized, with no plan versions and four launch-seed errors (`state.json:5,27,47,56,65,74`; `phase_result.json:6–10` says internal error and no written phase artifacts). This is no evidence of wrapper-delivered implementation. It does not establish that no other thread or prior source work exists. The anchor itself records recovered commits and four fire-19 GPU passes, while rejecting those passes as production/warm acceptance because of verification bypasses. Available Astrid history also contains the September 8 output-law commit `19020383` affecting `production_engine.py`. No tests, live GPU or other execution were run for this assessment, and no current P0–P7 closure is newly certified.

## Consequential source findings

| Finding in available source | Sequencing implication |
|---|---|
| Astrid `astrid/packs/vibecomfy/production_engine.py:194–202` calls `CheckoutServerAdapter.from_host_session` and `run_compiled_workflow`; no definitions of those methods were found in the current backend file, whose class starts at `astrid/core/generation/backends/vibecomfy.py:917`. | P0.b reconstruction/P2 real wiring remain a concrete dependency. A typed bridge call site is not an implemented owned-session path. Do not ask GEN to reconstruct it independently. |
| `production_engine.py:177–188` calls `run_embedded_sync`; its embedded profile still specifies `warm_policy: never` at line 22. | P6 changes the host-level lifetime/cancellation seam. GEN's Vibe output adapter can be designed early but final retained-embedded integration must consume P6 evidence. |
| `astrid/packs/wan2gp/src/driver.py:623–629,683–696` explicitly creates a one-shot per-attempt session with the spool bound to `output_dir`; `compiler.py:203–225,228–245` builds warmth identity from portable request settings. | P5 must own persistence, resident-key correction and per-task spool rebinding. A journal WARM label or compatible task fingerprint is not attested warm weights. GEN-T5 must preserve separate task output identity when it consumes the new session adapter. |
| Current VibeComfy `runtime/session.py:756–773` has `runtime_root/cwd`; `:1733–1755` contains launch-marker/process-birth checks. Current HEAD is `898c4e63`, differing from the successor's recorded merge inputs. | Some ingredients exist, but P1's exact content merge and accepted modern configuration closure cannot be inferred from current main. Reconcile `cb130f1b`/`5b39fea5` and their retained source identity; do not accept an ancestry-only merge or a keyword match. |
| Astrid `generic_host.py` is dirty and contains per-process cleanup, output upload and settlement. No `ManagedToolSession`/`SessionManager` implementation was found in the targeted current host/pack search. | P2 and GEN-T3/RRP-05 share a substantive lifecycle/publication seam, not merely neighboring files. One integration owner must sequence their patches. This bounded search does not claim no implementation exists in any other checkout. |

## Single-owner contracts

GPU P2 owns the **Worker → GenericPackHost → engine-neutral ManagedToolSession → engine children** custody design. Preserve all four declared modes: owned one-shot, owned persistent composite, explicitly adopted external service and third-party API (anchor lines 29–40). The original four projects must consume that capability descriptor; neither GEN nor RRP should introduce a separate process/session controller. RRP-03's launcher owns realm-start orchestration through DB's recovery operation; it is not the GPU session manager.

Runtime remains sole durable task/lease/reservation/settlement authority. DB owns realm schema/verifier/admission/epoch/recovery; GPU owns session incarnation, residency and execution admission generation. GEN owns generation/variant domain publication. In particular the GPU manager's `generation` token is an execution fence, **not a GEN generation row ID**. A request/result must retain both identities with unambiguous field names. Runtime lease/epoch validity and session admission-generation validity are both required before publishing results; neither check substitutes for the other. Define the fail-closed linearization between session fencing and settlement in P2/GEN-T2 integration, including cancellation/restart after output creation and before settlement. Do not persist a second session task ledger to solve the race.

Warm control is a task-independent performance policy within that manager. `resident_key` covers model/component digests and engine/memory/environment policy, not prompt, seed, resolution, output path or task ID (anchor lines 46–54). Per-task spool, admitted output grouping and result provenance remain task-specific. GEN's intent/partial-output envelope, GPU's ownership/residency capability descriptor, Runtime protocol digest and exact SQL format are four separate contracts. Jointly identify any required fields during DB-T1/GEN-T1/P0; only DB's Runtime owner changes schema/verifier, and only when evidence shows a durable field is needed. Existing F/P/L client handoffs accommodate the changes; session metadata is not permission for an unversioned schema relaxation.

GEN-T3's publication owner retains receipt-only harvest, managed objects and atomic domain publication; GPU adapters own producing and rebinding the private spool. RRP-05 owns filename requirements. Keep port/object/digest/filename distinct, canonicalize bytes before hashing, and never rewrite settlement digests to manufacture cold/warm equality. GPU P7/B04-T05 owns its producer/manifest convergence and deletions; any overlapping common manifest edit is implemented once by the shared publication owner and credited with both acceptance rows. Reigh migration/legacy Supabase lifecycle closure belongs to the GPU program, not GEN.

## Minimum handoffs and phase order

| GPU phase / existing requirement | Cross-portfolio handoff |
|---|---|
| P0 — recovery, ledger, composition preparation, budget/capacity/routing | Before shared source edits, reconcile actual repositories, historical accepted compositions, reopened correction and file custody with DB-T1/GEN-T1/RRP-01. P0.c is provisional until P1–P3 outputs exist, as the anchor's M1 explicitly says; do not invent a circular demand for final P1 output before P1 starts. |
| P1 — real Vibe content merge and independent review | Supplies accepted launch/config/binding primitives to P2. It can run in its authorized isolated Vibe checkout alongside DB Runtime work and V1-local files after P0 prerequisites; it is not a reason to block DB-T2. |
| P2 — manager core and independent review | **Session contract S:** reviewed ownership modes, seven operations, incarnation/resident/admission identities, current-session settlement rejection and CPU fault evidence. GEN-T3's host integration, GPU-facing bootstrap and any retained-session consumer require S. SDK/result and descriptor fixture work can precede S without claiming host integration complete. |
| P3 — session-aware harness; M1 composition frozen | **Composition handoff H:** S plus the exact composed host/adapter source, converted harness inventory and CPU integration evidence. This is the smallest accepted baseline for combined retained-host integration. Old tests that require every engine to die after every task cannot certify the new design. H releases integration, not live/warm claims. |
| P4 — checkout GPU proof | After P0–P3 and signed historical budget, prove cold/warm/cancel/identity/restart with task-bound outputs. Required for live checkout claims, not V1/RRP deterministic work. |
| P5 — Wan persistent runner and residency feasibility | After P4, supply the retained Wan spool/cancellation/resident-key implementation and real residency evidence to the affected GEN-T5 integration. GEN's one-shot/fixture correctness can be accepted within its actual scope earlier; do not call it persistent-GPU proof. |
| P6 — retained embedded session and feasibility | After P5, supply the retained embedded/cancellation seam to affected GEN-T5/Vibe integration. Flip warmth only on the specified evidence class. P5/P6 CPU *research* may prepare fixtures early; this plan does not reorder their implementation before P4. |
| P7 — CR2, producer/deletion closure, final freeze/CPU/GPU/recovery/accounting/review | Preserve all reopened/pending B03–B05 obligations and CR2 before final freeze. For a composition that includes DB/GEN changes, consume accepted F/P/L, shared-host handoffs and relevant affected checks before freezing/firing. P7 certifies that GPU composition; later source changes require affected revalidation, not retroactive reuse of a prior fire. |

Preserve P0 → P1 → P2 → P3 → P4 → P5 → P6 → P7. Allow other projects alongside this sequence; do not reinterpret the portfolio waves as permission to skip a phase. A narrow accepted S/H handoff is a consumer release, not GPU completion or closure of any pending 45-row obligation.

**Why not finish all GPU first?** That would idle unrelated DB schema/verification and V1/RRP selection work, then risk refreezing/refiring GPU after those projects change Runtime and settlement. **Why not run everything independently?** GEN-T3 and GPU P2/P6 would encode conflicting process lifetime, cancellation and output custody. The efficient point of synchronization is accepted CPU session/host handoffs, followed by one final GPU composition chosen deliberately. Do not force GEN cloud adapters to wait for physical Wan residency; do not promote a warm-GPU claim using cloud/CPU evidence.

## Custody, budget and remaining unknowns

DB's explicit existing-main/no-worktree amendment and GPU P1's ratified isolated-clean-worktree requirement both stand in their own scope. DB does not inherit GPU isolation; Vibe/Reigh/Worker do not inherit DB's main-only instruction. For shared Astrid/Runtime content, use an explicitly agreed source transfer and one write owner: GPU's isolated reviewed artifact can be consumed into DB's authorized main closure only after dirty-source reconciliation and a recorded integration decision. If simultaneous shared-file work cannot honor both mandates, defer the later shared edit; continue disjoint work. This assessment changes no custody lease, branch or run instruction.

The GPU program retains one pod at a time, at most two live attempts per engine/profile cluster, the **USD 6 aggregate cap including history**, three hours per pod, preprovisioned models, 4 GiB free-space floor, 2 GiB generated-evidence cap, provider-observed zero-pod cleanup and no production deployment (idea snapshot constraints; anchor P0/P7). No portfolio smoke may evade these limits by using a new task or budget label. Sol remains P0.b/P1/P2 implementer with implementer-independent Sol reviews/final review; Luna remains adapter/harness/evidence worker; routing and prior review counters are unchanged. This synthesis is not a GPU review or a charge against V1's two remaining rounds.

Before execution, resolve: available custody copies of the missing originals/archives and four recovered tips; actual accepted source composition versus the anchor's Runtime `70872d03`/Reigh `cf9d772be` and current dirty trees; whether S/H exists in another checkout with usable receipts; the precise session-fence/Runtime-settlement race contract; current wrapper launch-seed configuration if that wrapper is selected for resumption; historical spend/attempt/capacity balance. Wrapper repair is a harness task, not a prerequisite to write independent DB/V1 code or evidence that GPU product code failed. No recovery or wrapper fix was attempted here.

The first ready *planning* tranche is joint source/contract reconciliation plus disjoint fixture design. After the relevant execution authorization and custody gates, the first implementation tranche is GPU P0 → P1, DB-T2 → T3/RRP-02, V1-01 → V1-02 and RRP-04 on non-overlapping files. Then prioritize GPU P2 → P3 and GEN publication P so GEN-T3 integrates once against S/H. Reserve GPU P7's final physical proof for the accepted source composition, without holding the four projects' unrelated work or erasing the GPU program's full remaining obligations.
