# Adopt the running GPU project; integrate at its checkpoint

Confirmed receiving environment: a different MacBook with 8 GB RAM. The current GPU conversation is on that machine. Perform recovery there; the preparation-machine search found no verified current owner and does not imply that the destination project is stopped. No preparation-machine session ID is a valid substitute.

Latest user instruction: the running GPU project is under this orchestrator's remit immediately, not only after it finishes. It is the existing Astrid unified-execution program, not another project to launch. Recover and supervise its existing conversation and manager alongside the other programs. Do not create a duplicate owner or interrupt healthy work to change models.

Delegate a bounded conversation-recovery investigation using the installed pickup-agent-conversation/search-agent-logs skills. Match `megado-unified-v3`, inspect the actual recent tail and active control artifacts, and exclude this preparation conversation and historical planning wrappers. Record the verified conversation ID, host, cwd, control root, source set, current checkpoint and safe messaging/resume mechanism in the local manager registry. Do not publish raw transcripts or machine-local session pointers. A live owner must not be resumed in a second process. If no safe live messaging channel is available, observe its evidence and arrange the handoff at its idle checkpoint rather than creating another manager.

The user most recently said “R7”; the earlier confirmed milestone was **P7 CPU closure**. Verify the actual task ID with the existing owner and its current tasklist. Do not invent an R7 stage, assume completion from the wording, or restart earlier phases. Adopt newer verified progress if it has moved on.

## Required checkpoint handoff

1. **Supervise now.** Obtain the owner's concise read-back/status: current task, active workers, exact per-repository candidate and dirty inputs, tests still running, remaining CPU/physical GPU obligations, review counters and provider balance. Keep its existing work moving within those limits. Establish that its checkpoint stop is a handoff to the orchestrator, not portfolio completion.
2. **Freeze at the agreed checkpoint.** Once the real checkpoint's required evidence passes, have delegated source-custody workers preserve all intended project commits and uncommitted changes per repository in identifiable candidate refs/snapshots. Record exclusions and resolve overlapping writers. Do not discard or silently fold unrelated local work into the GPU candidate.
3. **Compose project work first, then merge main.** On isolated continuation branches, reconcile the GPU project's existing branches/deltas into its complete candidate. Fetch each repository's latest main—including the newly published local-source updates—and merge that main into the continuation candidate. Detect already-included changes to avoid applying them twice. This is not a reset to main or a transplant of dirt into a live checkout. Record both parents, conflict resolutions and the complete compatible revision set.
4. **Test the combined state.** Delegate affected Runtime/Astrid/generated-client/worker integration checks on that exact composition. Route defects and conflicts to their owning managers. Main having passed its own tests does not certify the merged GPU candidate. Retain unchanged evidence; rerun affected proofs and required reviews, within existing budgets. No billable rerun is authorized merely because a merge happened.
5. **Hand back a tested continuation.** The orchestrator judges the returned receipts and gives the existing UE manager the accepted continuation refs and original next task. Release overlapping portfolio work only under the existing ownership/dependency gates. Remaining GPU proof continues under UE's original limits. Final promotion to each main still requires the original acceptance/review and final Integration & E2E contract.

All source reconciliation, Git commands, test execution and bookkeeping above are delegated. The orchestrator owns sequencing and evidence judgment; the project manager owns its workers. Use bounded integration workers at this checkpoint; do not wait for the end-of-portfolio INT campaign to reconcile P7 inputs, and do not create a competing GPU manager.

Follow [source publication](source-publication.md), [repository custody](repositories.md), and [pure orchestration](orchestrator-contract.md). Publication of the preparatory main updates is distinct from acceptance or completion of the running GPU project.
