> Frozen reference snapshot. Current state and receiving authority are governed by the package START-HERE and authority.md; preserve the existing live run. Paths below are source references, not missing package requirements.

# Goal and authorization

Planning amendment: the user adopted the GPT-6 Astra medium assessment and requested the plan update. Keep the full C1–C8 outcome, prioritize consolidation of existing mechanisms, and use the provisional 6–9-day estimate with 10–15-day contingency in plan.md. Evidence: [astra-assessment.md](astra-assessment.md). No model bindings, review stages, criterion IDs or execution authority changed.

Prepare the referenced clean-break plan as a ready-to-start Megado project. User instruction: “don't execute yet. Get it ready to go”; subsequent “Go” continues that setup. This turn authorizes planning artifacts and read-only exploration only.

Delivery objective after authorization: convert the runtime and Astrid callers to one canonical database format and one lifecycle boundary, remove the listed compatibility machinery, and demonstrate all criteria in [implementation-criteria.md](implementation-criteria.md). Preserve existing domain and HTTP/client behavior except explicitly unsupported legacy input formats.

Direction: [northstar.md](northstar.md). Role bindings, oracle budget and future review stages: [run.yaml](run.yaml), the sole configuration declaration. Current host prepares the package as coordinator; designated oracle retains consequential judgment. The source plan supplies the initial architecture direction, so no initial oracle call is needed for setup. Unresolved architecture or source-authority conflicts go to that oracle during authorized delivery.

User custody amendment: “this should be inside the kind of main branch. Don't create new branches.” Keep setup and future implementation in the existing main checkout. Do not create branches or implementation worktrees. This overrides the skill's default isolated execution custody. Preserve unrelated dirty work, serialize overlapping edits and record per-task diffs/source hashes for evidence. Resolve runtime main-source location before editing; do not silently select a feature worktree or switch a dirty checkout. This amendment does not authorize starting execution.

Scope includes the owning runtime repository and its CLI/server lifecycle, catalog, generated clients, Astrid bootstrap and callers, tests and operator documentation. Resolve their exact source closure using source-state.md (original source reference: `source-state.md`). Dirty material is authoritative inspection input; a future HEAD-only checkout must not silently discard it.

Non-goals: diagnose the physical corruption trigger; salvage or migrate old realms; compatibility bridges; generalized storage infrastructure; new cloud infrastructure. Existing realms and backups remain untouched. Implementation may build explicit fresh-realm provisioning and replacement commands; operating them against real development or production data is separate from fixture validation.

Setup completes when coherent plan/tasks, criteria, source identity, assumptions, estimate and future launch instructions are durable and internally linked. No execution tests, implementation worktree or certification verdict is required or claimed. Future delivery completes with integrated executable evidence, disposition of blocking findings and passing declared gates. No inferred merge/push/deployment authority.

Resource limits: local disposable fixtures and focused suites during delivery; no GPU, paid infrastructure or live-service experiments. Unavailable configured models must be resolved explicitly before dispatch. Estimate and uncertainty are in plan.md.
