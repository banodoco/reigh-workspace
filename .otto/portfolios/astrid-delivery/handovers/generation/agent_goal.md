> Frozen reference snapshot. Current state and receiving authority are governed by the package START-HERE and authority.md; preserve the existing live run. Paths below are source references, not missing package requirements.

# Goal and authorization

Prepare a concrete executable plan for aligning all audited Astrid generation paths with canonical runtime generation/variant bookkeeping. Current authorization is **planning only**: user said "$megado set up this for execution, don't execute yet". Stop after planning artifacts; do not implement, run execution tests, create execution worktrees, start providers, repair the live runtime, merge, push or deploy.

Direction: [northstar.md](northstar.md). Sole role/stage/budget declaration: [run.yaml](run.yaml). Scope/criteria: [plan.md](plan.md). Assignments: [tasklist.md](tasklist.md). Status and consumed calls: [status.md](status.md).

Planning source: source-state.json (original source reference: `source-state.json`), current Astrid and runtime working trees INCLUDING dirty changes; Reigh is read-only context for the GPU plan. Prior factual audits: ../astrid-generation-path-audit-20260910/. Uncommitted material is included in planning, not silently discarded in favor of HEAD. No immutable execution composition has been selected.

First useful outcome: Codex and standard fal generation appear via canonical generation/variant queries with correct media/task provenance, even when the original caller exits. Full agreed scope also covers OpenAI images, fal H3/Foley, generic local generation, Wan2GP and explicitly generative Vibe workflows, using one shared completion path. Existing successful output and timeline workflows must remain usable.

Existing users/consumers: Astrid SDK and CLI/runtime queries, typed generation facade, specialized packs, and Reigh producers represented in the GPU plan. Real workspace data exists and must be preserved. No deployment is authorized; use disposable runtime fixtures during later delivery. RunPod tooling is substrate, not a generator merely because it pulls a file.

Non-goals: GPU route migration/provisioning/warm-session project; live-realm corruption repair; direct database writes; historical-record backfill; guaranteed no duplicate paid provider work after interruption; automatic timeline placement; UI redesign; new generic workflow framework. Missing historical metadata is not fabricated.

No paid/live provider or GPU spend is authorized by this planning run. Future live acceptance needs an explicit selected disposable target and a concrete approved provider/resource cap. Existing earlier test results are background evidence only, not candidate certification.

Estimate: 3–5 focused engineer-days for shared durable publication/facade; 5–8 total for audited path alignment. Agent elapsed time is not equivalent and depends on runtime readiness, source reconciliation and provider access. Resume only on explicit user execution authorization; read Megado execution mechanics then and establish isolated custody without losing concurrent dirty work.
