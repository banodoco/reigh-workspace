> Frozen reference snapshot. Current state and receiving authority are governed by the package START-HERE and authority.md; preserve the existing live run. Paths below are source references, not missing package requirements.

# Direction

Every supported Astrid generation produces discoverable project-owned generation and variant records, linked to its canonical task, events, receipts and managed media. Completion remains truthful under asynchronous execution, retries, cancellation and crashes. The same runtime authority serves Codex, cloud and GPU executors.

Preserve existing prompts, reference ordering, model/provider/session provenance, output order and explicit timeline editing. Reuse existing domain records, runtime lifecycle, storage and host. Do not create a second queue, database, poller, backend-specific registration service, automatic timeline insertion or implicit provider fallback.

This direction authorizes no implementation, deployment, live-realm repair or GPU provisioning. Source: user requests in this conversation and ../astrid-generation-path-audit-20260910/summary.md.
