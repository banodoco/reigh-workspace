> Frozen reference snapshot. Current state and receiving authority are governed by the package START-HERE and authority.md; preserve the existing live run. Paths below are source references, not missing package requirements.

# Decision summary

The preserved Astra oracle ruling is `RR-ORACLE-001`: approve the simplified integration direction, revise the design before implementation, and use existing task/receipt/CAS machinery rather than a review-specific database or lifecycle service.

Portable consequences:

- Render authority is the exact manifest/CAS identity and digest tuple, never a local path.
- Rendered decoded duration controls evidence. Preserve any excess tail; call it unmapped unless pixel evidence proves blackness.
- Allowed frozen spoken metadata remains independent of shot labels. Prompts and unmarked transcripts are excluded; missing metadata means no spoken-text metadata, not inferred silence.
- The default result is one bounded full-duration overview with mandatory first/last/tail-transition evidence and adaptive interior sampling. Exhaustive boundaries remain index/drill-down data; every shot boundary is not promised in the bounded overview.
- Range, density, and spatial resolution are independent request values. A stateless range has no fabricated parent; manifest-derived navigation carries ancestry and rejects identity conflicts.
- Reuse generic execution/artifact events and receipts. Do not add a review event family or a manifest/event digest cycle.
- Remove speculative CLI commands and flags until the parser exposes them.

The source response is retained in the original checkout at `.otto/archive/render-review-plan-20260910/oracle-response.md`; this concise summary is the portable decision record and does not claim implementation or test evidence. The original central budget was three oracle calls, one consumed by this ruling, leaving two shared calls. No oracle call was made during this setup handover.
