> Frozen reference snapshot. Current state and receiving authority are governed by the package START-HERE and authority.md; preserve the existing live run. Paths below are source references, not missing package requirements.

# North Star

Ship a trustworthy existing `timelines visualize --view filmstrip` Render Review: exact managed-render identity, decoded full duration including any excess tail, spoken-only metadata, bounded full-duration overview, independent range/density/resolution, and honest navigation/coverage.

Reuse today's admission, manifests, run/task events, CAS publication, host settlement and disposable rehydration. Do not anticipate Plan 2 with a lifecycle adapter, new storage framework, new event family, or speculative CLI.
