> Frozen reference snapshot. Current state and receiving authority are governed by the package START-HERE and authority.md; preserve the existing live run. Paths below are source references, not missing package requirements.

# Portable delivery setup goal

This is the authoritative planning-only handover for [Plan 1](plan.md). It is ready for a later delivery run, but no implementation, worktree, tests, deployment, model UX run, or review stage has been launched by setup.

Scope: render identity/digest rejection, decoded duration/tail, spoken-only captions, full overview, independent range/density/resolution, honest coverage/navigation, current CLI compatibility, existing cache/publication behavior, and a bounded agent UX evaluation covering authored structure, rendered filmstrip, and the cross-view journey. Plan 1 completes independently; Plan 2 owns the later lifecycle migration and remains a separate control run.

UX delivery sequence, when explicitly authorized: baseline fresh Luna actors → Astra friction assessment 1 → Luna-high precise fix pass 1 → fresh replay of the affected task → Astra verification 2 → Luna-high precise fix pass 2 → final fresh deterministic/held-out check. Each Astra UX round is one capped invocation and is not an oracle call or hidden final review. Missing image viewing/capture is undetermined. Sol is reserved for a demonstrated hard kernel after decomposition.

Source custody is the dirty `Astrid` checkout at commit `82d09bb91c8a4621695c1d8b6966652cad941ef4` on `main`; the selected dirty timeline/runtime patch and exclusions are recorded in [source-provenance.md](source-provenance.md). This directory is the single authoritative run home and export source.
