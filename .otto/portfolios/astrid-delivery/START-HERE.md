# Astrid portfolio — orchestrator handover

Mandatory latest direction: read [the pure orchestration contract](orchestrator-contract.md). Astra low coordinates; Luna xhigh managers delegate all substantive execution; Astra high supplies sparse meta-oracle judgment within the existing budget. Give the startup read-back before dispatch. Operational instructions to inspect, edit, test, sync or merge mean delegate those actions, then judge the returned evidence.

You are coordinating six existing programs to tested, validated, integrated code on their respective `main` branches. Five are new projects; **Astrid unified-execution**, plan **`megado-unified-v3`**, is already at **P7 CPU closure**. Continue that work; do not restart it from an older wrapper or planning snapshot.

The intended recipient is an intelligent Codex coordinator on the same Mac, opening a persistent Codex window for each new project manager and keeping progress visible in those same threads. Use judgment, delegate real work, and keep the state small. This package is direction and evidence, not a new scheduling framework.

One additional **Integration & E2E manager** coordinates their compatible multi-repository finish; it is not a seventh product implementation. Read [repository custody and push strategy](repositories.md) and [V1 fixture readiness](fixture-readiness.md). The visualization fixture is a required early build task, not an already packaged runnable video.

## First ten minutes

Fetch this package from the published handover branch into a separate, unused directory if needed:

```sh
git clone --branch handover/astrid-portfolio-20260910 --single-branch \
  https://github.com/banodoco/reigh-workspace.git <unused-handover-checkout>
git -C <unused-handover-checkout> rev-parse HEAD
```

Compare the SHA with the preparer's delivery message. This checkout supplies the package; it is not a replacement for the existing multi-repository workspace or live run state. Do not check out the handover branch over a dirty workspace. Read [dependency setup](dependencies/README.md), fetch the pinned GitHub skills and run its verified local-only sync on the receiving machine before dispatch. Inspect existing skill targets rather than overwriting them.

1. Read [authority](authority.md), [plan](plan.md), [operations](operations.md), and [the project index](projects.md). Read the pinned [Megado skill](dependencies/megado/SKILL.md) before dispatching delivery.
2. Find the existing unified-execution manager and its current P7 handoff. Record the actual thread, candidate source, remaining CPU closure, review counters and live-proof obligations. An old `active` status or a failed wrapper is not its current state.
3. Read [source publication](source-publication.md). Distinguish the published current-main source snapshot from the separate P7 candidate. Neither is automatically the combined starting point.
4. Use the single portfolio control root `<workspace>/.otto/portfolios/astrid-delivery/`. Reuse its current state; never replace it from an older copy. Activate the receiving `delivery` mode under [the user's mandate](authority.md), preserving role bindings and counters from [run.yaml](run.yaml). Individual project runs remain under their existing `.otto/runs/` roots.
5. Open one manager thread per ready new project, using the verified launch instructions in [operations](operations.md). Give each its [manager prompt](assets/project-manager-message.md), project handover and exact allowed source scope. Queue overlapping implementation until the P7 merge checkpoint passes; research and genuinely independent work can proceed.
6. Arrange the first hourly check in the same orchestrator thread, record the mechanism and next check, and send the user a short update naming what is running, what is queued, and the next gate.

## What you are handing to managers

| Program | Role in the portfolio | Starting action |
|---|---|---|
| Unified-execution (UE) | Existing host/session, typed engine and task execution work across Astrid, Runtime, Reigh integration and VibeComfy | Continue current P7 CPU closure with its existing manager; obtain the handoff, not a new duplicate manager |
| Database clean break (DB) | Canonical Runtime schema, admission, transactions, recovery and client closure | Source/contract inventory now; shared implementation after the merge checkpoint unless explicitly included in P7 |
| Generation alignment (GEN) | Atomic generation/variant publication and canonical results across producers | Agree contract inputs now; consume DB and accepted UE boundaries |
| Timeline visualization (V1) | Exact-render filmstrips, truthful coverage, navigation and spoken metadata | Prepare fixture and independent work; preserve original task and UX ordering |
| Render-path reliability (RRP) | Responsive diagnostics, launcher recovery, scoped render resolution and verified named opening | Trace and contribute shared requirements; implement once with the boundary owner |
| Ephemeral derived artifacts (E2) | Runtime-owned temporary derivatives, safe retention/promotion/deletion; primary outputs remain durable | Design output intent/schema with DB/GEN; final visualization migration follows V1 acceptance |
| Integration & E2E (INT) | Exact cross-repository composition, practical final setup/workflow and coordinated main promotion | After the continuation checkpoint, prepare and consume accepted handoffs early; run the final campaign only on settled code |

The detailed [sequencing analysis](analysis/portfolio-plan.md) and [orchestrator brief](analysis/orchestrator-brief.md) explain the dependencies. **This handover's authority and merge checkpoint are the latest instructions** where older analysis says delivery permission is absent or suggests a different source-custody policy.

## What completion means

Every agreed project is implemented, required tests and declared reviews pass, the final compatible composition is merged to each affected repository's `main`, and the remote SHAs are verified. Report remaining live/environment limitations honestly; an unproved mandatory criterion prevents claiming that project complete. UE's supported honest terminal is not successful delivery of the portfolio.

No production deployment, existing data migration, new provider budget or unrelated repository rewrite is implied. Existing live-resource limits and review budgets remain binding.

Use [the receiving prompt](assets/handover-message.md) to start the orchestrator. The preparation process publishes this package and selected source; it does not start the orchestrator, managers or hourly monitor.
